// Fixture syncing is handled server-side via a Supabase cron job (every 2 min).
// Realtime subscriptions in MatchCenterView push DB updates to clients automatically.
export function useLiveSync() {}
